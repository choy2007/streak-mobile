import React, { Component } from 'react';
import styles from '../../styles/top-scorer';

export default class TopScorer extends Component {
  render() {
    return (
      <Text> TOP SCORER COMPONENT </Text>
    )
  }
}
